<form action="<%= post_comments_path(post) %>" method="post", placeholder="texto" >

  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <textarea name="comment[content]" rows="3" cols="20" value, class ="form-control mb-3", placeholder = "adicionar comentário"></textarea>

  <input type="submit" value="submit", class= "btn btn-primary">
</form>
