<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.text_field :tittle, class:"form-control mb-3", placeholder:"Título *" %>
  <%= form.text_area :content, class:"form-control mb-3", placeholder:"Texto *" %>
  <%= form.text_area :image, class:"form-control mb-3", placeholder:"link da imagem *" %>

  <small class="form-text text-muted d-block">(*) Campos obrigatórios</small>

  <%= form.submit class:"btn btn-primary mt-2"  %>
<% end %>